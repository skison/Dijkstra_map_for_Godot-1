# Default Rust output target; don't commit directly, instead carry over to addons/bin folder when ready
/target

# Rust dependencies lock file - not needed, just make sure new builds work/pass tests
Cargo.lock

# Godot 4+ specific ignores
.godot/

# Godot-specific ignores
.import/
export.cfg
export_presets.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# IDEs
.idea/
.vs/
.vscode/

# Python virtual environment
.venv/

# Windows temp files (e.g. on .dll files when project is open in Godot)
~*